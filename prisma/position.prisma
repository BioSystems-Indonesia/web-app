model Position {
    id          String  @id @default(uuid())
    code        String  @unique
    name        String
    description String?

    employees Employee[]
    jobdesks  Jobdesk[]

    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt
    deletedAt DateTime?
}
